# Copy to docker/.env and set your local values
POSTGRES_DB=fooddb
POSTGRES_USER=food
POSTGRES_PASSWORD=changeme

# OpenSearch dev admin password (for local only)
OS_PASS=changeme

# Optional ports override (uncomment to change)
# KAFKA_PORT=9092
# OPENSEARCH_PORT=9200
# CONNECT_PORT=8083
